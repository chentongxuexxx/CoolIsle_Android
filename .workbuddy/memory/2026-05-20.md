# 2026-05-20 工作日志

## 第五阶段：稳定性与合规

### 完成内容

1. **Splash Screen API 升级**
   - 添加 `androidx.core:core-splashscreen:1.0.1` 依赖
   - 创建 `ic_splash_background.xml`、`ic_splash_foreground.xml`、`mipmap-anydpi-v26/ic_splash.xml`
   - 更新 `themes.xml` 为 `Theme.SplashScreen` 父主题
   - 修改 `MainActivity.onCreate()` 使用 `installSplashScreen()`

2. **通知权限申请（Android 13+ 合规）**
   - Manifest 添加 `POST_NOTIFICATIONS` 权限
   - MainActivity 添加 `requestNotificationPermission()` 方法
   - 使用 `ActivityResultContracts.RequestPermission()` 处理权限请求

3. **Firebase Crashlytics 集成**
   - 添加 `firebase-crashlytics-ktx` 依赖
   - gradle.properties 添加 `crashlytics.build.id` 配置
   - CI workflow 添加 Crashlytics Build ID 注入步骤
   - CI 添加 `crashlyticsUploadSymbolsRelease` 符号表上传

4. **ProGuard 规则更新**
   - 添加 Firebase Crashlytics 保留规则
   - 添加 Splash Screen API 保留规则
   - 添加 FCM Service 保留规则
